---
# Source: helm for kubernetes/templates/ingress.yaml
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: juju                  # Ingress name
  labels:
    app: juju
spec:
  ingressClassName: nginx   # Optional ingressClass
  rules:
    - host: "juju.smartops.space"
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: juju               # Links to Service name dynamically
                port:
                  number: 8080  # Links to service port dynamically
